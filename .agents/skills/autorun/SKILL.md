---
name: autorun
scope: project
---

Before JARVIS product or UX work, read the repository-root project instruction:

- `JARVIS_V4_PROJECT_INSTRUCTIONS.md`

The full canonical contracts are maintained separately in the Harness repository. This local project instruction is the clone-portable Electron coverage and must not depend on an absolute path outside this repository.

Working rules:

- The current user request outranks historical roadmaps; actual current code outranks historical reports.
- JARVIS is currently in the V4 interaction / UX redesign phase.
- Do not automatically continue paused milestones.
- Preserve stable backend contracts and do not perform unrelated cleanup.
- Do not begin paused milestones or V4 implementation without an explicit request.
- No push, PR, or merge without an explicit request; no destructive Git operations.
